# Build artefacts
*.zip

# Editors
.idea/
.vscode/
*.swp
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# PHP tooling — the module ships no Composer dependencies on purpose, so that vendor/ can never
# collide with the versions PrestaShop itself loads.
/vendor/
composer.lock
.php_cs.cache
.php-cs-fixer.cache
